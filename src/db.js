const { Pool } = require('pg');let pool = null;function getPool() {    if (!pool) {        if (process.env.DATABASE_URL) {            pool = new Pool({                connectionString: process.env.DATABASE_URL,                ssl: { rejectUnauthorized: false },                max: 5,                idleTimeoutMillis: 30000,                connectionTimeoutMillis: 10000            });        } else {            pool = new Pool({                host: process.env.DB_HOST || 'localhost',                user: process.env.DB_USER || 'root',                password: process.env.DB_PASS || '',                database: process.env.DB_NAME || 'proteccion_mta',                port: parseInt(process.env.DB_PORT) || 5432,                max: 5,                idleTimeoutMillis: 30000,                connectionTimeoutMillis: 10000            });        }    }    return pool;}async function query(sql, params = []) {    const pool = getPool();    const result = await pool.query(sql, params);    return result.rows;}async function insert(sql, params = []) {    const pool = getPool();    const result = await pool.query(sql, params);    return result.rows[0]?.id || null;}async function testConnection() {    try {        const pool = getPool();        const client = await pool.connect();        client.release();        return true;    } catch (err) {        console.error('Error de conexión a DB:', err.message);        return false;    }}async function initDatabase() {    const pool = getPool();    const createTables = `        CREATE TABLE IF NOT EXISTS admins (            id SERIAL PRIMARY KEY,            email VARCHAR(255) NOT NULL UNIQUE,            password VARCHAR(255) NOT NULL,            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP        );        CREATE TABLE IF NOT EXISTS scripts (            id SERIAL PRIMARY KEY,            nombre VARCHAR(255) NOT NULL,            script_key VARCHAR(64) NOT NULL UNIQUE,            estado VARCHAR(10) DEFAULT 'activo' CHECK (estado IN ('activo', 'inactivo')),            ip_servidor VARCHAR(45) DEFAULT '',            puerto VARCHAR(10) DEFAULT '22003',            duracion VARCHAR(20) DEFAULT 'permanente',            fecha_expiracion TIMESTAMP DEFAULT NULL,            sftp_host VARCHAR(255) DEFAULT '',            sftp_port INT DEFAULT 22,            sftp_user VARCHAR(255) DEFAULT '',            sftp_pass VARCHAR(255) DEFAULT '',            sftp_path VARCHAR(255) DEFAULT '',            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP        );        CREATE TABLE IF NOT EXISTS servidores (            id SERIAL PRIMARY KEY,            script_id INT NOT NULL REFERENCES scripts(id) ON DELETE CASCADE,            ip_servidor VARCHAR(45) NOT NULL,            puerto VARCHAR(10) DEFAULT '22003',            propietario VARCHAR(255) DEFAULT '',            estado VARCHAR(10) DEFAULT 'activo' CHECK (estado IN ('activo', 'inactivo')),            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP        );        CREATE TABLE IF NOT EXISTS logs (            id SERIAL PRIMARY KEY,            script_id INT,            ip_servidor VARCHAR(45),            accion VARCHAR(255),            resultado VARCHAR(10) DEFAULT 'denegado' CHECK (resultado IN ('permitido', 'denegado')),            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP        );        CREATE TABLE IF NOT EXISTS settings (            id SERIAL PRIMARY KEY,            clave VARCHAR(100) NOT NULL UNIQUE,            valor TEXT DEFAULT '',            updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP        );    `;    try {        await pool.query(createTables);        console.log('[DB] Tablas verificadas/creadas correctamente');        const admins = await pool.query('SELECT COUNT(*) as total FROM admins');        if (parseInt(admins.rows[0].total) === 0) {            await pool.query(                "INSERT INTO admins (email, password) VALUES ($1, $2)",                ['exe.zip44@gmail.com', '$2y$10$n/XXN9ESFqJUZSvzAw/YYuPpLCJiR7XDufk9zR6M8IXnXRvrKhypK']            );            console.log('[DB] Admin por defecto creado: exe.zip44@gmail.com');        }        return true;    } catch (err) {        console.error('[DB] Error creando tablas:', err.message);        return false;    }}module.exports = { query, insert, testConnection, getPool, initDatabase };