const express = require('express');const db = require('../db');const crypto = require('crypto');const router = express.Router();function generateKey() {    return crypto.randomBytes(16).toString('hex').toUpperCase();}function calcularExpiracion(duracion) {    const now = new Date();    switch (duracion) {        case '1_semana':   now.setDate(now.getDate() + 7); break;        case '2_semanas':  now.setDate(now.getDate() + 14); break;        case '1_mes':      now.setMonth(now.getMonth() + 1); break;        case '3_meses':    now.setMonth(now.getMonth() + 3); break;        case '6_meses':    now.setMonth(now.getMonth() + 6); break;        case '1_ano':      now.setFullYear(now.getFullYear() + 1); break;        case 'permanente': return null;        default: return null;    }    return now.toISOString().slice(0, 19).replace('T', ' ');}router.get('/', async (req, res) => {    try {        const rows = await db.query('SELECT * FROM scripts ORDER BY created_at DESC');        res.json({ status: 'success', scripts: rows });    } catch (err) {        console.error('Error listando scripts:', err.message);        res.status(500).json({ status: 'error', message: 'Error al listar scripts' });    }});router.get('/:id', async (req, res) => {    try {        const rows = await db.query('SELECT * FROM scripts WHERE id = $1', [req.params.id]);        if (rows.length === 0) {            return res.status(404).json({ status: 'error', message: 'Script no encontrado' });        }        res.json({ status: 'success', script: rows[0] });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al obtener script' });    }});router.post('/', async (req, res) => {    try {        const { nombre, ip_servidor, puerto, duracion } = req.body;        if (!nombre || !ip_servidor) {            return res.status(400).json({ status: 'error', message: 'Nombre e IP son obligatorios' });        }        const key = generateKey();        const port = puerto || '22003';        const dur = duracion || 'permanente';        const fechaExp = calcularExpiracion(dur);        await db.query(            'INSERT INTO scripts (nombre, script_key, ip_servidor, puerto, duracion, fecha_expiracion) VALUES ($1, $2, $3, $4, $5, $6)',            [nombre, key, ip_servidor, port, dur, fechaExp]        );        await db.query(            "INSERT INTO logs (ip_servidor, accion, resultado) VALUES ($1, 'Script creado via API', 'permitido')",            [ip_servidor]        );        res.json({ status: 'success', message: 'Script creado', key });    } catch (err) {        console.error('Error creando script:', err.message);        res.status(500).json({ status: 'error', message: 'Error al crear script' });    }});router.put('/:id', async (req, res) => {    try {        const { nombre, ip_servidor, puerto } = req.body;        const id = req.params.id;        const rows = await db.query('SELECT * FROM scripts WHERE id = $1', [id]);        if (rows.length === 0) {            return res.status(404).json({ status: 'error', message: 'Script no encontrado' });        }        const updates = [];        const params = [];        let paramIdx = 1;        if (nombre) { updates.push(`nombre = $${paramIdx++}`); params.push(nombre); }        if (ip_servidor !== undefined) { updates.push(`ip_servidor = $${paramIdx++}`); params.push(ip_servidor); }        if (puerto) { updates.push(`puerto = $${paramIdx++}`); params.push(puerto); }        if (updates.length > 0) {            params.push(id);            await db.query(`UPDATE scripts SET ${updates.join(', ')} WHERE id = $${paramIdx}`, params);        }        await db.query(            "INSERT INTO logs (script_id, ip_servidor, accion, resultado) VALUES ($1, 'panel', 'Script actualizado via API', 'permitido')",            [id]        );        res.json({ status: 'success', message: 'Script actualizado' });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al actualizar script' });    }});router.post('/:id/toggle', async (req, res) => {    try {        const id = req.params.id;        await db.query(            "UPDATE scripts SET estado = CASE WHEN estado = 'activo' THEN 'inactivo' ELSE 'activo' END WHERE id = $1",            [id]        );        const rows = await db.query('SELECT estado FROM scripts WHERE id = $1', [id]);        const nuevoEstado = rows.length > 0 ? rows[0].estado : '?';        await db.query(            "INSERT INTO logs (script_id, ip_servidor, accion, resultado) VALUES ($1, 'panel', $2, 'permitido')",            [id, `Estado cambiado a ${nuevoEstado}`]        );        res.json({ status: 'success', message: `Estado: ${nuevoEstado}`, estado: nuevoEstado });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al cambiar estado' });    }});router.post('/:id/regenerate', async (req, res) => {    try {        const id = req.params.id;        const newKey = generateKey();        await db.query('UPDATE scripts SET script_key = $1 WHERE id = $2', [newKey, id]);        await db.query(            "INSERT INTO logs (script_id, ip_servidor, accion, resultado) VALUES ($1, 'panel', 'Key regenerada via API', 'permitido')",            [id]        );        res.json({ status: 'success', message: 'Key regenerada', key: newKey });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al regenerar key' });    }});router.post('/:id/renovar', async (req, res) => {    try {        const id = req.params.id;        const { duracion } = req.body;        const dur = duracion || 'permanente';        const fechaExp = calcularExpiracion(dur);        if (fechaExp) {            await db.query(                "UPDATE scripts SET duracion = $1, fecha_expiracion = $2, estado = 'activo' WHERE id = $3",                [dur, fechaExp, id]            );        } else {            await db.query(                "UPDATE scripts SET duracion = 'permanente', fecha_expiracion = NULL, estado = 'activo' WHERE id = $1",                [id]            );        }        await db.query(            "INSERT INTO logs (script_id, ip_servidor, accion, resultado) VALUES ($1, 'panel', $2, 'permitido')",            [id, `Licencia renovada: ${dur}`]        );        res.json({ status: 'success', message: `Licencia renovada: ${dur}` });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al renovar' });    }});router.delete('/:id', async (req, res) => {    try {        const id = req.params.id;        await db.query(            "INSERT INTO logs (script_id, ip_servidor, accion, resultado) VALUES ($1, 'panel', 'Script eliminado via API', 'permitido')",            [id]        );        await db.query('DELETE FROM scripts WHERE id = $1', [id]);        res.json({ status: 'success', message: 'Script eliminado' });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al eliminar' });    }});router.get('/logs/all', async (req, res) => {    try {        const limit = parseInt(req.query.limit) || 100;        const rows = await db.query('SELECT * FROM logs ORDER BY created_at DESC LIMIT $1', [limit]);        res.json({ status: 'success', logs: rows });    } catch (err) {        res.status(500).json({ status: 'error', message: 'Error al obtener logs' });    }});module.exports = router;